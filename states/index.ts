import AUTH_STATE from './auth';
import TRIP_STATE from './trip';
import RATING_STATE from './rating';

const states = {
  auth: AUTH_STATE,
  trip: TRIP_STATE,
  rating: RATING_STATE,
};

export default states;
